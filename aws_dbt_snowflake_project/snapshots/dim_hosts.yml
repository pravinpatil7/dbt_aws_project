snapshots:
    - name: dim_hosts
      relation: ref('hosts')
      config:
        schema: gold
        database: '{{ target.database }}'  
        unique_key: host_id
        strategy: timestamp
        updated_at: created_at
        dbt_valid_to_current: "to_date('2099-12-31')" 